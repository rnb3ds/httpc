// Package types provides shared type definitions used across the httpc library.
package types

// Note: RetryPolicy, MiddlewareFunc, Handler, RequestMutator, ResponseMutator,
// FormData, and FileData types are defined in interfaces.go and form.go.
// Config struct is defined in the main package (types.go) with flat structure.
